<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <!-- 发送邮箱验证码 -->
    <chain name="/core/service/email/send">
        THEN(
            /** 校验邮箱格式 **/
            EmailValidChecker,
            /** 基于 remoteIp + email 的简单限流 **/
            EmailRateLimitChecker,
            /** 生成验证码、写入缓存并发送邮件 **/
            EmailCodeSender
        );
    </chain>
</flow>
