任务名称：
项目与客户端基准 API 理解梳理

任务目标：
在不修改正式代码与正式文档的前提下，完成对当前项目架构、模块职责、现有 API 面、`docs/t` 客户端基准协议及其与仓库现状关系的系统理解，并形成后续 API 重写前的证据基础。

任务背景：
用户要求先阅读相关文档和代码，完整理解项目；并说明 `docs/t` 下是从客户端带来的基准 API，后续需要以该基准 API 为标准重写项目的 API 设计。

任务类型：
只读探索 / 审查任务

影响模块：
- `application-starter`
- `chat-domain`
- `infrastructure-basic`
- `infrastructure-service`
- `docs/`
- `docs/t/`

允许修改范围：
- 仅允许在 `ai-agent-workplace/` 新增或更新任务单与分析记录

禁止修改范围：
- 不修改正式源码
- 不修改正式测试
- 不修改正式配置
- 不修改 `docs/` 正式规范与协议文档
- 不调整模块职责、依赖方向与对外协议

依赖限制：
- 不新增任何第三方依赖

配置限制：
- 不新增或修改运行时配置

文档依据：
- `docs/AI协作开发规范.md`
- `docs/变更审核清单.md`
- `docs/任务单模板.md`
- `docs/架构文档.md`
- `docs/包结构规范.md`
- `docs/依赖引入规范.md`
- `docs/API.md`
- `docs/t/SERVER_API.md`
- `docs/t/10-http-ws-protocol-v1.md`
- `docs/t/11-http-endpoints-v1.md`
- `docs/t/12-ws-events-v1.md`
- `docs/t/13-error-model-and-reasons-v1.md`
- `docs/t/14-pagination-and-cursor-v1.md`

任务分解 / 执行计划：
1. 阅读 AI 协作、任务单、审核清单与架构文档，锁定执行边界。
2. 阅读 `docs/t` 与现有 `docs/API.md`，识别客户端基准协议与当前服务端整理文档的关系。
3. 读取多模块代码入口、控制器、应用服务、仓储适配与启动装配，确认当前实现落点。
4. 抽取项目的模块边界、协议风格、错误模型、WS 模型和测试覆盖情况。
5. 输出项目理解摘要、后续 API 重写的关键约束与需要用户确认的协议变更边界。

关键假设与依赖：
- `docs/t/` 为后续 API 重写设计的主要外部基准。
- 现有仓库已做过多轮 `docs/t` 对齐，但“后续要以客户端基准 API 为标准重写设计”意味着仍可能存在设计层面的重构空间。
- 本任务仅做证据收集与理解，不宣称实现或协议已经最终正确。

实现要求：
- 结论必须有仓库内文档或代码证据支撑。
- 不以历史任务单替代源码与正式文档结论。

测试要求：
- 本任务为只读探索 / 审查任务，本次无代码改动，因此不适用实现类测试要求。

质量门禁：
- 已完成必要文档与代码阅读。
- 结论有仓库内证据链支撑。
- 明确说明本次无正式代码改动。
- 明确说明未执行测试 / 构建的原因。

复审要求：
- 对后续涉及对外协议重写的事项，必须在进入实现前取得用户明确确认。

文档要求：
- 本次如无新增长期规则，不修改 `docs/`。

验收标准：
- 能清晰说明项目当前模块结构与职责。
- 能清晰说明 `docs/t` 在项目中的地位与当前 `docs/API.md` 的关系。
- 能指出后续 API 重写前必须锁定的关键协议边界。

完成定义：
- 完成阅读与总结。
- 向用户输出结构化理解结果。
- 记录本次为只读探索任务，未进行代码修改与实现类验证。

实际结果：
- 已完成项目规范文档、当前 API 文档、`docs/t` 客户端基准协议文档与多模块关键入口代码阅读。
- 已确认项目为重写阶段的 Maven 多模块 Spring Boot 后端：`application-starter` 负责启动装配，`chat-domain` 承载业务与协议入口，`infrastructure-basic` 承载固定基础设施，`infrastructure-service/*` 承载可替换外部服务。
- 已确认 `chat-domain` 当前在同一代码库内并存两套 API 风格：一部分 v1 端点直接返回资源对象 / `204` / 标准错误对象；另一部分历史/过渡接口仍返回 `CPResponse` 业务包装。
- 已确认 `docs/t` 表达的是客户端基准 v1 HTTP/WS 协议，而 `docs/API.md` 表达的是“当前仓库实际实现整理”，两者不能等同。
- 已确认后续若“以客户端基准 API 为标准重写项目 API 设计”，本质上属于对外协议层整体重构，不应按零散补接口处理。

验证记录：
- 不适用。本任务为只读探索 / 审查任务，本次无代码改动，因此不适用实现类测试 / 构建门禁。

残留风险：
- 当前已完成关键入口与协议层核验，但尚未逐个深挖全部领域对象、数据库 service 契约与所有测试断言细节；若后续进入实现，需要在具体 API family 上补充针对性下钻。

知识沉淀 / 是否回写 docs：
- 暂不回写 `docs/`，待后续若形成稳定长期规则再判断。

产物清理与保留说明：
- 保留本任务单作为后续 API 重写前的探索记录。

补充说明：
- 后续若进入正式 API 重写设计或实现，需要更新或新建实现类任务单，并在涉及对外协议变更前取得用户确认。
