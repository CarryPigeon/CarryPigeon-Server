任务名称：

PRD 覆盖矩阵整理

任务目标：

基于 `docs/产品需求文档.md` 与当前仓库已实现能力，整理一份可持续维护的产品需求覆盖矩阵，明确哪些需求已实现、部分实现、未实现或当前阶段不在范围内。

任务背景：

- 用户询问产品需求文档是否已全覆盖，并明确希望得到一份覆盖矩阵。
- 当前仓库已经完成群治理、消息撤回、文件/语音消息、JWT 鉴权、实时通信等能力，但产品需求文档范围远大于当前实际交付范围。
- 需要把“代码现状”与“PRD 要求”之间的关系沉淀为稳定文档，便于后续排期和验收。

影响模块：

- `docs/产品需求文档.md`
- `docs/`
- `ai-agent-workplace/`

允许修改范围：

- 允许新增稳定规划文档到 `docs/`
- 允许在 `ai-agent-workplace/` 维护本任务单

禁止修改范围：

- 不修改生产代码
- 不凭空发明 PRD 中不存在的产品能力
- 不把前端/客户端能力误判为后端已实现

文档依据：

- `AGENTS.md`
- `docs/AI协作开发规范.md`
- `docs/任务单模板.md`
- `docs/产品需求文档.md`
- `docs/变更审核清单.md`

任务分解 / 执行计划：

1. 拆解 PRD 中与当前后端能力相关的需求条目。
2. 对照当前代码、测试和已完成任务单，判断实现状态。
3. 输出稳定的 markdown 覆盖矩阵到 `docs/`。
4. 补充结论、差距摘要和后续规划建议。

实现要求：

- 覆盖矩阵按需求粒度组织，而不是只按大章节泛化描述。
- 状态固定为：`Implemented`、`Partial`、`Not Implemented`、`Out of Current Scope`。
- 每条已实现或部分实现项都应给出明确证据来源。

质量门禁：

- 覆盖矩阵能区分“已实现”和“仅有方向/架构意图”。
- 文档适合后续 roadmap 使用。
- 任务单记录实际结果与残留风险。

实际结果：

- 已基于 `docs/产品需求文档.md`、当前仓库代码、测试与已完成任务单，整理出稳定的 PRD 覆盖矩阵文档。
- 最终产物位于：`docs/产品需求文档覆盖矩阵.md`。
- 矩阵按需求粒度而非泛化章节组织，并使用 `Implemented / Partial / Not Implemented / Out of Current Scope` 四类状态。
- 已把当前高覆盖能力、部分实现能力、明确未实现能力以及下一阶段规划候选一并整理到矩阵中。

验证记录：

- 已阅读并对照：`docs/产品需求文档.md`。
- 已对照当前后端实现与测试证据，包括但不限于：
  - `chat-domain` 中 auth / channel / message / server 相关主链路代码
  - `application-starter/src/main/resources/application.yaml`
  - `ai-agent-workplace/20260424-110921-ai-group-chat-permission-recall-done.md`
  - `ai-agent-workplace/20260424-220500-ai-test-governance-phase3-done.md`
- 已生成最终稳定文档：`docs/产品需求文档覆盖矩阵.md`。

残留风险：

- 覆盖矩阵基于当前后端仓库，不包含客户端/前端实现状态。
- 对于 PRD 中带有“潜在能力”“后续阶段目标”性质的条目，矩阵采用保守口径，避免把方向性内容误记为已实现。
- 后续如新增稳定能力，需要同步更新该矩阵，否则矩阵会滞后于实际实现。

知识沉淀 / 是否回写 docs：

- 本任务的最终产物直接写入 `docs/`。

产物清理与保留说明：

- 本任务单已关闭为 `done`。
- 最终稳定产物保留在 `docs/产品需求文档覆盖矩阵.md`。
