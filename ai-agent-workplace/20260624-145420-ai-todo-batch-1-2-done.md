任务名称：
TODO 第一二四组联合处理

任务目标：
完成三组已确认 TODO 的联合落地：一是处理基建暴露方式相关 TODO 与现行文档基线的冲突，二是按确认方向删除 cache-api 中仅用于测试且当前不再保留的模型类型，三是把第 4 项中原计划暂缓的结构与落点调整一并纳入本轮。

任务背景：
用户已确认代码注释中的 TODO 均为正式任务；当 TODO 与文档冲突时，需要先与用户确认基准。当前已完成冲突讨论，并得到以下决策：
- 基建暴露方式不统一收口到 `InfrastructureBasics`，保持当前“门面 + 直接暴露”的混合模型；
- cache-api 中 `CacheEntry`、`CacheKey`、`CacheTtl` 按 TODO 方向删除；
- 第一组与第二组合并在同一轮任务内实施；
- 用户进一步要求将上一版方案中的第 4 项也纳入本轮，即：
  - `InitializationCheckRunner` 迁移；
  - `database-api` / `database-impl` 结构重整；
  - `toRecord` / `toEntity` 归属调整。

影响模块：
- `infrastructure-basic`
- `infrastructure-service/cache-api`
- `infrastructure-service/database-api`
- `infrastructure-service/database-impl`
- `application-starter`
- `docs/基建文档.md`
- `docs/配置规范.md`
- `docs/架构文档.md`
- `docs/包结构规范.md`

允许修改范围：
- 允许修改与本次 TODO 直接相关的正式源码、测试与必要文档
- 允许删除 `cache-api` 中已确认不保留的模型类型及对应测试
- 允许清理与已确认基线冲突的 TODO 注释
- 允许同步更新引用这些类型的模块说明文档
- 允许在 `database-api` / `database-impl` 内进行围绕 TODO 的结构重整
- 允许迁移 `InitializationCheckRunner` 的类落点与装配位置

禁止修改范围：
- 不修改模块依赖方向
- 不引入新第三方依赖
- 不引入未经确认的新架构模式
- 不把 `chat-domain` 改为依赖任何 `*-impl`

依赖限制：
- 仅使用现有 Maven 依赖

配置限制：
- 不新增运行时配置项
- 不新增未来占位配置

文档依据：
- `AGENTS.md`
- `docs/AI协作开发规范.md`
- `docs/变更审核清单.md`
- `docs/任务单模板.md`
- `docs/架构文档.md`
- `docs/包结构规范.md`
- `docs/依赖引入规范.md`
- `docs/基建文档.md`
- `docs/配置规范.md`
- `docs/测试规范.md`
- `docs/注释规范.md`

任务分解 / 执行计划：
1. 明确第一组 TODO 的落地策略：保持当前基建暴露模型，不为 `Ids`、`LogContexts` 强行引入 `InfrastructureBasics` 门面改造。
2. 逐一处理第一组冲突 TODO：
   - `Ids`、`LogContexts`：清理与已确认基线冲突的 TODO 注释；
   - `TimeFormats`、`ConfigurationPrefix`：结合引用现状决定删除或保留，并同步文档。
3. 删除 `cache-api` 中 `CacheEntry`、`CacheKey`、`CacheTtl` 及其测试，并同步更新模块说明，移除对这些最小契约模型的描述。
4. 处理 `DatabaseMapperScanConfiguration` 下沉到 `database-impl`，并与 `database-impl` 自动配置边界保持一致。
5. 迁移 `InitializationCheckRunner`，将共享启动检查执行器调整到与本轮确认边界一致的落点，并同步修正装配与测试。
6. 按 TODO 对 `database-api` / `database-impl` 进行结构重整，优先以“一个 service 及其对应 model 相邻组织”为目标，但保持改动最小且不破坏现有契约。
7. 将 `MybatisPlus*DatabaseService` 中的 `toRecord` / `toEntity` 映射逻辑迁移到更合适的落点，优先考虑实体侧静态转换或同等单一职责实现，并保证实现风格在受影响服务中保持一致。
8. 补充并运行受影响测试，完成文档和任务单自检。

关键假设与依赖：
- “同步构建 1、2 任务一起完成”指将“基建暴露方式冲突处理”与“cache-api model 删除”合并到同一轮正式实施。
- “将 4 也纳入本轮”按整体理解处理，即把上一版方案第 4 项中的三类暂缓任务全部并入。
- 第一组中 `Ids` / `LogContexts` 的 TODO 以用户确认后的基线处理，即不再按 TODO 字面继续推进门面收口。
- `TimeFormats` 与 `ConfigurationPrefix` 是否保留，允许在实现前通过引用与文档自检后作最小化判断；若删除，需要同步更新相关文档。
- `database-api` / `database-impl` 的结构重整以当前 TODO 指向为准，但仍需控制在本轮可验证范围内，避免借机扩张为全面目录翻修。

实现要求：
- 保持 `InfrastructureBasics` 仅聚合注入型固定能力，不把纯静态辅助能力机械塞入门面。
- 删除 `cache-api` 模型后，不得引入新的替代膨胀契约。
- `DatabaseMapperScanConfiguration` 下沉后，Mapper 扫描职责必须留在 `database-impl`。
- `InitializationCheckRunner` 迁移后，`application-starter` 仍只负责启动装配与运行前编排，不承载具体外部检查逻辑。
- `database-api` / `database-impl` 结构重整必须保持依赖方向不变，且不为形式一致性过度扩张层级。
- 映射逻辑迁移后，service 层应更接近薄适配器职责。

测试要求：
- 删除类与配置迁移必须补充或调整对应测试。
- 受影响测试至少覆盖：
  - `application-starter` 启动装配边界
  - `database-impl` 自动配置边界
  - `cache-api` 删除模型后的测试收敛结果
  - `InitializationCheckRunner` 迁移后的启动检查装配与行为
  - 映射逻辑迁移后的受影响 database service 契约

质量门禁：
- 受影响源码编译通过
- 受影响测试通过
- 不新增架构、依赖、配置违规
- 文档与代码基线不再相互矛盾
- 跨模块结构调整后，自动配置与依赖方向仍符合架构文档
- 任务单记录实际验证命令与结果

复审要求：
- 对 `InfrastructureBasics` 暴露边界、cache-api public contract 收缩、启动检查执行器落点、Mapper 扫描落点、database 结构重整边界进行深度自检

文档要求：
- 若删除 `TimeFormats` 或 `ConfigurationPrefix`，必须同步更新对应正式文档
- 若 cache-api 模块说明发生变化，需同步更新 `package-info` 或相关模块说明
- 若 `InitializationCheckRunner` 或 database 模块结构落点变化影响长期规则，需同步更新相关正式文档

验收标准：
- 第一组与第二组 TODO 在已确认边界内完成落地
- 第四组中纳入的三类任务在不破坏依赖边界前提下完成落地
- 代码中不再保留与当前确认基线冲突的 TODO
- cache-api 三个模型类型及其测试已删除，且说明文档已同步
- Mapper 扫描配置已迁移至 `database-impl`
- 启动检查执行器与 database 结构整理已完成，并有测试或编译证据支撑

完成定义：
- 代码、测试、必要文档与任务单记录全部完成
- 质量门禁执行并记录
- 任务单从 `current` 改为 `done`

实际结果：
- 已完成 `InitializationCheckRunner` 从 `application-starter` 迁移到 `infrastructure-basic.startup`
- 已完成 `DatabaseMapperScanConfiguration` 下沉为 `database-impl` 内部自动配置 `DatabaseMapperScanAutoConfiguration`
- 已删除 `cache-api` 中的 `CacheEntry`、`CacheKey`、`CacheTtl` 及对应测试
- 已删除 `TimeFormats`、`ConfigurationPrefix`
- 已清理 `Ids`、`LogContexts` 中与当前确认基线冲突的 TODO 注释
- 已将 `database-api` 中 auth/user 相关 service 与 record 调整为按能力邻近组织：
  - `auth.account`
  - `auth.session`
  - `user.profile`
- 已将 `database-impl` 中 auth/user 相关 MyBatis entity / mapper / service 调整为按能力邻近组织
- 已将 `AuthAccountEntity`、`AuthRefreshSessionEntity`、`UserProfileEntity` 承担记录转换职责，相关 service 收敛为更薄适配器
- 已同步更新受影响正式文档

验证记录：
- `rg -n "TODO|todo|Todo" application-starter/src/main/java infrastructure-basic/src/main/java infrastructure-service -g '!**/test/**'`
  - 结果：实现结束后无剩余源码 TODO
- `git diff --check`
  - 结果：通过，无空白错误
- `mvn -pl infrastructure-basic,infrastructure-service/database-api,infrastructure-service/database-impl,application-starter test -DskipTests=false`
  - 结果：未能完成；环境无法解析 `maven.aliyun.com`
- `mvn -o -Dmaven.repo.local=/root/.m2/repository -pl infrastructure-basic,infrastructure-service/database-api,infrastructure-service/database-impl,application-starter test -DskipTests=false`
  - 结果：未能完成；离线仓库缺失 `cn.hutool:hutool-core:5.8.36`

残留风险：
- 本轮未完成 Maven 测试验证，原因是当前环境依赖解析不可用，仍需在可联网或具备完整本地仓库的环境中复验
- `database-api` / `database-impl` 仅对 auth/user 相关能力做了能力邻近组织；其余 capability 仍保留旧结构，后续若继续推进相同 TODO，需要单独立项
- `CacheBackedEmailVerificationCodeServiceTests` 中的方法名包含 `deletesCacheEntry`，这里的 `CacheEntry` 仅为英文语义文本，不是类型引用，无需改动

知识沉淀 / 是否回写 docs：
- 已回写 `docs/基建文档.md`
- 已回写 `docs/配置规范.md`
- 已回写 `docs/架构文档.md`
- 已回写 `docs/包结构规范.md`

产物清理与保留说明：
- 保留本任务单作为正式实施边界记录

补充说明：
- 当前任务已完成代码与文档落地，但受环境依赖限制，验证闭环尚缺 Maven 测试通过证据。
