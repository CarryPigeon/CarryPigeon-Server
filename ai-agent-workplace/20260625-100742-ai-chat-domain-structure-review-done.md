任务名称：
chat-domain 结构与架构审查

任务目标：
基于项目既有架构与包结构规范，对 `chat-domain` 的结构清晰度、架构合理性、可读性与可扩展性进行只读审查，并给出带证据的问题结论。

任务背景：
用户要求先阅读相关文档和代码，再聚焦 `chat-domain`，判断其是否达到成熟开源项目在结构清晰、架构合理、可读性和可扩展性上的标准。

影响模块：
- `chat-domain`
- `docs/`
- `ai-agent-workplace/`

允许修改范围：
- 允许新增与更新 `ai-agent-workplace/` 下的审查任务单
- 允许读取仓库文档、源码与测试

禁止修改范围：
- 不修改正式 Java 代码、测试代码、配置和 `docs/`
- 不调整模块职责、依赖方向和包结构
- 不新增第三方依赖

依赖限制：
- 仅使用仓库现有工具与只读命令收集证据

配置限制：
- 不新增或修改任何运行时配置

文档依据：
- `docs/架构文档.md`
- `docs/包结构规范.md`
- `docs/依赖引入规范.md`
- `docs/注释规范.md`
- `docs/测试规范.md`
- `docs/AI协作开发规范.md`
- `docs/变更审核清单.md`

任务分解 / 执行计划：
1. 阅读项目架构、包结构、AI 协作与审核规范，提炼本次审查基准。
2. 梳理 `chat-domain` 目录、包层级、feature 边界和与其他模块的依赖接点。
3. 抽样阅读关键 feature 的 application/domain/controller/support/config 代码与测试。
4. 按严重级别输出审查结论，明确问题、证据、风险与优先级。

关键假设与依赖：
- 假设当前仓库代码可代表用户希望评审的最新状态。
- 假设本次任务属于只读探索 / 审查任务，不要求执行实现类测试/构建门禁。

实现要求：
- 审查结论必须以仓库内文档和源码为依据。
- 重点关注 `chat-domain` 的 feature 分层、共享边界、仓储适配位置、协议层与应用层职责。
- 审查输出采用代码审查格式，优先列问题而不是泛化总结。

测试要求：
- 本任务为只读探索 / 审查任务，不新增测试，不执行“为改动验证而运行”的实现类测试。

质量门禁：
- 已完成必要文档与源码阅读。
- 结论具备可定位到文件与行号的证据链。
- 明确说明本次无正式代码改动。
- 明确说明未执行实现类测试/构建门禁的原因。

复审要求：
- 结合架构文档与包结构规范进行深度自检。

文档要求：
- 不回写 `docs/`，除非发现需要沉淀的新长期规则；本次默认不新增长期规则。

验收标准：
- 给出 `chat-domain` 是否结构清晰、架构合理的明确判断。
- 至少覆盖可读性、可扩展性、边界清晰度、共享代码治理四个维度。
- 所有关键结论均附仓库内证据。

完成定义：
- 输出正式审查结论。
- 任务单补充实际结果、验证记录和残留风险后改为 `done`。

实际结果：
- 已完成 `docs/` 规范阅读、`chat-domain` 包结构梳理、关键 feature 抽样阅读与跨 feature import 统计。
- 结论：`chat-domain` 当前总体方向符合仓库规范，已经具备 feature-first 分包、应用/领域/控制/支撑分层、领域仓储抽象与 database-api 薄适配等基础成熟度；但尚未达到成熟开源项目在可读性和可扩展性上的稳定标准。
- 主要问题集中在：跨 feature 直接依赖偏多、`server/message/auth/user/channel/file` 之间存在较重编排耦合；控制器和运行时类直接依赖其它 feature 的 application/support 层；部分生产类存在为测试或兼容服务的旁路构造器；应用层抽象父类依赖面过宽。

验证记录：
- 只读阅读：`docs/架构文档.md`、`docs/包结构规范.md`、`docs/依赖引入规范.md`、`docs/注释规范.md`、`docs/测试规范.md`、`docs/AI协作开发规范.md`、`docs/变更审核清单.md`。
- 只读阅读：`chat-domain/pom.xml` 与 `chat-domain/src/main/java/team/carrypigeon/backend/chat/domain/**` 关键类。
- 只读统计：`chat-domain` main Java 文件约 303 个，test Java 文件约 73 个。
- 只读统计：跨 feature import 中 `message->channel` 约 32 处、`server->message` 约 26 处、`channel->message` 约 8 处、`channel->user` 约 8 处、`message->user` 约 8 处。
- 本任务为只读探索 / 审查任务，本次无正式代码改动，因此未执行实现类测试、构建或启动验证。

残留风险：
- 本次为抽样审查和结构性统计，不等同于逐行审计所有业务行为。
- 未运行测试/构建；因为没有正式代码改动，测试结果不能作为本次结论必要证据。

知识沉淀 / 是否回写 docs：
- 暂不回写 `docs/`。当前问题属于现有规范执行成熟度不足，不需要新增长期规则。

产物清理与保留说明：
- 保留本任务单作为审查追踪记录。

补充说明：
 - 本任务为只读探索 / 审查任务，本次无代码改动，因此不适用实现类质量门禁。
