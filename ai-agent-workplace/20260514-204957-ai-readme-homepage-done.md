任务名称：
升级 README 首页

任务目标：
将根 `README.md` 从偏内部入口的简版说明升级为更像开源项目首页的结构，同时保持对当前项目重写阶段、运行边界和能力现状的诚实描述。

任务背景：
当前项目的 Swagger 门户已经相对完整，但根 README 仍主要是内部跳转入口，不利于首次到访的前端、测试或外部协作方快速理解项目定位与上手路径。

影响模块：
- `README.md`

允许修改范围：
- 允许重写根 `README.md` 的结构和文案

禁止修改范围：
- 不允许虚构生产成熟度、发布流程或未实现能力
- 不允许新增新文档文件替代 README

依赖限制：
- 不新增依赖

配置限制：
- 不新增配置

文档依据：
- `docs/API.md`
- `docs/部署手册.md`
- `docs/架构文档.md`
- `AGENTS.md`

任务分解 / 执行计划：
1. 重新组织 README 结构：项目简介、状态、特性、模块、快速开始、Swagger、文档索引。
2. 保留重写阶段与当前边界说明，避免过度承诺。
3. 运行回归验证，确保文档改动不影响整体交付状态。

关键假设与依赖：
- 当前 README 目标读者包括开发者、测试、联调方与潜在外部协作方。

实现要求：
- 更像项目首页，而不是目录跳转页
- 结构清晰，文案简洁真实

测试要求：
- 至少运行受影响 reactor 完整测试作为收尾验证

质量门禁：
- `mvn -pl "application-starter" -am -DskipTests=false test` 通过

复审要求：
- 最终输出需明确说明 README 只是首页增强，不代表项目成熟度已改变

文档要求：
- 内容需与现有部署、API 与架构文档一致

验收标准：
- 新读者可以从 README 直接理解项目定位、模块结构、Swagger 入口与快速上手流程

完成定义：
- README 重写完成并验证通过
- 任务单改为 `done`

实际结果：
- 根 `README.md` 已重写为更像项目首页的结构
- 新首页包含项目简介、当前状态、主要特性、模块结构、快速开始、Swagger / OpenAPI、文档索引、当前边界说明与协作入口
- 文案明确保留“重写阶段”“开发/内网联调优先”“非生产级成熟开源成品”的真实边界
- README 升级方向与只读探索结论一致：更像首页，但不夸大项目成熟度或生产可用性

验证记录：
- `mvn -pl "application-starter" -am -DskipTests=false test`：通过
- README 重写后再次执行 `mvn -pl "application-starter" -am -DskipTests=false test`：通过
- 探索结论确认当前 README 升级方向应更像首页，但不能夸大项目成熟度，最终改写与该建议一致

残留风险：
- 当前首页已显著优于入口清单式 README，但仍未补齐真正开源项目常见的 `LICENSE`、`CONTRIBUTING` 等公共治理文件

知识沉淀 / 是否回写 docs：
- 已直接回写根 `README.md`

产物清理与保留说明：
- 当前任务单保留在 `ai-agent-workplace/`，状态已关闭为 `done`
