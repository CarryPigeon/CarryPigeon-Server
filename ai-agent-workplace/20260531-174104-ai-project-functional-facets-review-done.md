任务名称：

项目功能切面梳理与设计审查

任务目标：

基于当前仓库文档与代码，梳理项目主要功能切面，形成适用于本项目的功能切面审查清单，并对当前设计做逐项审查，给出证据、结论、风险与后续关注点。

任务背景：

用户要求先完整理解项目，再整理项目在持久化、事务、聊天、事件广播、日志系统等方面的设计切面，并构建审查清单逐项审查当前设计。

影响模块：

- `application-starter`
- `chat-domain`
- `infrastructure-basic`
- `infrastructure-service`
- `docs/`
- `ai-agent-workplace/`

允许修改范围：

- 允许在 `ai-agent-workplace/` 新增本次探索/审查任务单与必要的审查记录

禁止修改范围：

- 不修改正式业务代码、正式测试、正式配置、正式文档
- 不修改模块职责、依赖方向、包结构规则
- 不引入任何新依赖

依赖限制：

- 仅使用仓库现有代码与文档进行只读分析

配置限制：

- 不新增、不调整任何运行时配置

文档依据：

- `docs/架构文档.md`
- `docs/包结构规范.md`
- `docs/依赖引入规范.md`
- `docs/基建文档.md`
- `docs/配置规范.md`
- `docs/异常与错误码规范.md`
- `docs/测试规范.md`
- `docs/AI协作开发规范.md`
- `docs/变更审核清单.md`
- `docs/任务单模板.md`

任务分解 / 执行计划：

1. 阅读 AI 协作、审核、任务单规范，确认本次任务属于探索/审查类任务。
2. 阅读架构、包结构、依赖、基建、配置、异常、测试等项目文档，建立审查维度。
3. 阅读多模块代码与关键测试，提取功能切面与实现落点。
4. 归纳项目功能切面及其设计边界、协作关系、关键约束。
5. 构建切面审查清单，并对当前设计逐项审查，输出证据、结论、风险。
6. 在任务单记录实际结果与验证说明，并将任务单改为 `done`。

关键假设与依赖：

- 假设当前仓库代码与文档代表用户希望审查的最新设计基线。
- 假设本次任务只要求分析与审查，不要求提出并落地架构改造。

实现要求：

- 输出应以仓库内文档、源码、测试为证据
- 审查重点覆盖持久化、事务、聊天、事件广播、日志、配置、异常、测试、启动装配等功能切面
- 审查结论需区分设计优点、当前缺口、潜在风险

测试要求：

- 本任务为只读探索/审查任务，本次无代码改动，因此不适用实现类测试补充要求

质量门禁：

- 已完成必要文档阅读
- 已完成关键代码与测试阅读
- 结论具备仓库内证据链
- 明确说明本次无正式代码改动
- 明确说明未执行测试/构建的原因

复审要求：

- 对跨模块边界、持久化、事务、事件广播、日志等架构敏感切面进行深度自检

文档要求：

- 不改动正式 `docs/`
- 在 `ai-agent-workplace/` 保留任务单作为探索审查记录

验收标准：

- 给出项目功能切面总览
- 给出面向本项目的功能切面审查清单
- 给出逐项审查结论、证据、风险与建议关注点

完成定义：

- 最终输出已覆盖主要功能切面与审查结果
- 任务单已记录实际结果、验证记录、残留风险、知识沉淀判断
- 任务单已改为 `done`

实际结果：

- 已完成项目治理文档、架构文档、配置/异常/测试规范、关键模块代码、主要迁移脚本与代表性测试分布的阅读。
- 已按仓库当前实现梳理主要功能切面：模块装配、持久化、事务、认证鉴权、聊天频道治理、消息与提及、实时事件广播、对象存储、缓存预留、启动检查、日志、异常响应、配置与测试。
- 已形成逐项审查结论，并识别出若干高风险设计问题：
  - realtime 事件重放未按接收者过滤，存在跨用户事件泄露风险
  - realtime `reauth` 未清理旧账户绑定，单连接可残留多账户订阅
  - mention 创建事件在数据库事务内提前广播，可能产生回滚后幽灵事件
  - system channel 由注册流程懒创建且缺少唯一性约束，存在并发重复创建风险
  - discovery 文档固定返回 `wss://` 地址与固定 capability，和默认 realtime 关闭配置存在漂移
- 本次未修改正式代码、正式测试、正式配置与正式文档，仅新增 AI 任务单。

验证记录：

- 本任务为只读探索/审查任务，未执行 Maven 构建、测试或启动验证。
- 替代验证方式：
  - 阅读并交叉比对 `docs/` 规范、`pom.xml` 与多模块代码
  - 阅读 `application-starter` 配置与 Flyway 迁移脚本确认运行时与数据模型
  - 阅读 `chat-domain` 关键应用服务、实时通道、异常映射与持久化适配器
  - 阅读 `infrastructure-basic` 与 `infrastructure-service` 的关键自动配置、健康检查、事务与实现代码
  - 阅读代表性测试分布与数量，确认当前测试覆盖重心
  - 关键证据集中于：
    - `RealtimeChannelHandler`
    - `RealtimeSessionRegistry`
    - `MessageApplicationService`
    - `AuthApplicationService`
    - `ServerApplicationService`
    - `application.yaml`
    - `db/migration/*.sql`

残留风险：

- 本次结论基于静态阅读，未通过真实运行时复现实时链路问题。
- 当前工作树存在大量用户既有未提交修改，本次未触碰，也未尝试归因这些修改是否与审查问题相关。
- 若后续要把审查结论转为整改任务，需要单独形成实现类任务单并重新确认修改边界。

知识沉淀 / 是否回写 docs：

- 本次未直接回写 `docs/`。
- 但若后续确认继续沿用当前 realtime / 响应协议设计，建议把以下长期规则显式写回正式文档：
  - realtime 事件恢复是否允许跨连接重放，以及必须携带哪些接收者边界
  - `reauth` 的连接身份切换语义
  - 统一 HTTP 响应模型究竟是 `CPResponse` 还是当前 REST + `ApiErrorResponse` / 资源响应模型
  - system channel 的初始化责任应由迁移/启动阶段承担还是业务注册阶段承担

产物清理与保留说明：

- 保留本任务单作为探索/审查留痕，不产生其他临时文件

补充说明：

- 本任务为只读探索/审查任务，本次无代码改动，因此不适用实现类质量门禁。
