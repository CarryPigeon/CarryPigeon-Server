任务名称：

第 6 轮：测试、文档与最终收口

任务目标：

在前几轮实现完成后，统一补齐测试、正式 API 文档、自检记录与残留风险说明，完成任务闭环。

任务背景：

协议改造跨越多轮，如果没有统一收口，容易留下文档滞后、测试碎片化和边界遗漏问题。

影响模块：

- `chat-domain`
- `application-starter`
- `docs/`
- `ai-agent-workplace/`

允许修改范围：

- 正式测试
- 正式 API 文档
- 必要的少量修正代码
- 任务单关闭记录

禁止修改范围：

- 不引入新范围的大改
- 不在本轮追加未确认的新需求

依赖限制：

- 不新增无关依赖

配置限制：

- 仅允许为测试或文档准确性所需的最小调整

文档依据：

- `docs/变更审核清单.md`
- `docs/API.md`
- `docs/t/*`

任务分解 / 执行计划：

1. 运行并补齐相关测试。
2. 更新正式 API 文档。
3. 按变更审核清单执行自检。
4. 汇总残留风险与未完成项。
5. 把已完成任务单改为 `done`。

关键假设与依赖：

- 依赖前序实现轮次完成。

实现要求：

- 文档必须反映实际代码，而不是目标设计。

测试要求：

- 运行与受影响模块匹配的测试与构建验证。

质量门禁：

- 测试通过。
- 正式 API 文档已更新。
- 自检结论完整可追踪。

复审要求：

- 最终需要进行一次跨模块深度自检。

文档要求：

- `docs/API.md` 必须与新协议一致。

验收标准：

- 代码、测试、文档与任务单状态一致。

完成定义：

- 正式文档更新完成。
- 必要测试通过。
- 各轮任务单可关闭为 `done`。

实际结果：

- 待执行

验证记录：

- 待执行

残留风险：

- 若前几轮遗留语义不一致，本轮会暴露集中修补成本。

知识沉淀 / 是否回写 docs：

- 需要根据实际改造结果评估并回写长期规则。

产物清理与保留说明：

- 保留必要追踪产物，清理无用临时草稿
