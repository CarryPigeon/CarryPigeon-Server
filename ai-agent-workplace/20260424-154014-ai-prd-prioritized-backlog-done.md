任务名称：

PRD 优先级待办清单整理

任务目标：

基于现有 PRD 覆盖矩阵，整理一份按优先级排序、可直接用于后续排期的后端待办清单。

任务背景：

- 已完成 `docs/产品需求文档覆盖矩阵.md`，明确了当前后端对 PRD 的覆盖情况。
- 用户进一步要求把覆盖矩阵转化为按优先级排序的待办清单。
- 需要输出稳定规划文档，便于后续 roadmap 与迭代执行。

影响模块：

- `docs/`
- `ai-agent-workplace/`

允许修改范围：

- 允许新增稳定规划文档到 `docs/`
- 允许在 `ai-agent-workplace/` 维护本任务单

禁止修改范围：

- 不修改生产代码
- 不新增 PRD 中不存在的产品能力
- 不把前端/客户端事项混入当前后端待办

文档依据：

- `AGENTS.md`
- `docs/AI协作开发规范.md`
- `docs/任务单模板.md`
- `docs/产品需求文档.md`
- `docs/产品需求文档覆盖矩阵.md`

任务分解 / 执行计划：

1. 从覆盖矩阵中提取 `Partial` / `Not Implemented` / 与当前后端直接相关的能力。
2. 按“缺失核心契约优先于后续增强能力”的原则做优先级排序。
3. 生成稳定 markdown 文档到 `docs/`。

实现要求：

- 只整理后端待办，不扩展客户端范围。
- 每个待办项需说明：PRD 映射、当前状态、建议范围、依赖关系、优先级理由。
- 文档应适合直接作为后续排期输入。

质量门禁：

- 待办清单与覆盖矩阵保持一致。
- 优先级划分逻辑清晰、可解释。
- 产物位于 `docs/`，可长期维护。

实际结果：

- 已基于 `docs/产品需求文档覆盖矩阵.md` 整理完成按优先级排序的后端待办清单。
- 最终产物位于：`docs/产品需求文档优先级待办清单.md`。
- 清单已按 `P0 / P1 / P2` 分组，并为每项补充 PRD 映射、当前状态、建议范围、依赖关系与排序理由。

验证记录：

- 已阅读并对照：`docs/产品需求文档覆盖矩阵.md`。
- 已结合当前仓库上下文，按“缺失核心契约优先于后续增强能力”的规则完成排序。
- 已生成最终稳定文档：`docs/产品需求文档优先级待办清单.md`。

残留风险：

- 清单仅覆盖当前后端仓库相关事项，不包含客户端/前端范围。
- 若覆盖矩阵后续更新，本待办清单也需要同步维护，否则优先级会滞后于真实现状。

知识沉淀 / 是否回写 docs：

- 最终产物直接写入 `docs/`。

产物清理与保留说明：

- 本任务单已关闭为 `done`。
- 最终稳定产物保留在 `docs/产品需求文档优先级待办清单.md`。
