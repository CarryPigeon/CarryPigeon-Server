任务名称：

项目理解与客户端基准 API 基线梳理

任务目标：

阅读当前项目的关键规范文档、客户端基准 API 文档与主要协议实现代码，形成后续按客户端基准 API 重写服务端 API 设计所需的现状认知基线。

任务背景：

用户说明 `docs/t` 下文档来自客户端，并要求后续以客户端基准 API 为标准重写项目 API 设计。为避免后续实现偏离现有架构边界或误判当前协议现状，需要先完成只读探索。

影响模块：

- `docs/`
- `docs/t/`
- `chat-domain`
- `application-starter`
- `infrastructure-basic`

允许修改范围：

- 仅允许在 `ai-agent-workplace/` 新增或更新任务单

禁止修改范围：

- 不修改正式源码
- 不修改正式测试
- 不修改正式配置
- 不修改 `docs/` 长期规则文档

依赖限制：

- 不新增任何依赖

配置限制：

- 不修改任何运行时配置

文档依据：

- `docs/AI协作开发规范.md`
- `docs/任务单模板.md`
- `docs/架构文档.md`
- `docs/包结构规范.md`
- `docs/API.md`
- `docs/t/SERVER_API.md`
- `docs/t/10-http-ws-protocol-v1.md`
- `docs/t/11-http-endpoints-v1.md`
- `docs/t/12-ws-events-v1.md`
- `docs/t/13-error-model-and-reasons-v1.md`
- `docs/t/14-pagination-and-cursor-v1.md`

任务分解 / 执行计划：

1. 阅读 AI 协作、任务单、架构、包结构与当前 API 文档，确认仓库级边界。
2. 阅读 `docs/t` 客户端基准 API 文档，提炼协议核心约束。
3. 阅读当前 HTTP 控制器、统一响应、异常处理、服务发现与 WS 相关代码，确认现状落地。
4. 输出项目理解与“当前实现 vs 客户端基准”的关键差距。

关键假设与依赖：

- 本任务属于只读探索/审查任务，不进入正式代码实现。
- 当前仓库源码与 `docs/t` 文档共同构成后续 API 重写的输入边界。
- 若后续要真正按 `docs/t` 重写对外协议，将涉及对外协议变更，属于需要显式确认后再编码的范围。

实现要求：

- 结论必须以仓库内文档和源码为依据。
- 重点关注模块职责、统一协议模型、鉴权方式、错误模型、分页模型和 WS 事件模型。

测试要求：

- 本任务为只读探索任务，不要求运行测试。

质量门禁：

- 已完成关键文档阅读。
- 已完成关键协议实现代码阅读。
- 输出结论能指出当前实现和客户端基准 API 的关键差异。
- 明确说明本次无正式代码改动、无测试执行。

复审要求：

- 暂不需要独立复审；后续进入 API 重写方案或实现前需再次核对协议边界。

文档要求：

- 本次不修改 `docs/`，仅在 `ai-agent-workplace/` 留存任务单。

验收标准：

- 能准确说明项目模块结构、当前协议模型与 `docs/t` 基准 API 的主要差异点。

完成定义：

- 已形成任务单。
- 已完成只读探索。
- 已向用户输出项目理解摘要与后续 API 重写的关键约束提醒。

实际结果：

- 已阅读项目规范文档、当前 API 文档、客户端基准 API 文档与当前主要控制器/统一响应/异常/WS 代码。
- 已确认当前项目是重写阶段的多模块 Spring Boot 后端，业务核心位于 `chat-domain`，对外协议当前仍以 `CPResponse` 包装模型为中心。
- 已确认客户端基准 API 与当前实现存在系统性差异，后续不能按“局部补接口”思路处理，而应按协议层整体重构设计。

验证记录：

- 只读命令：`rg --files`
- 只读命令：`find docs -maxdepth 2 -type f | sort`
- 只读命令：`sed -n ...` 阅读关键文档与源码
- 本任务为只读探索任务，本次无代码改动，因此不适用实现类质量门禁，也未执行测试/构建

残留风险：

- 目前结论建立在关键入口与协议层代码之上，尚未逐个深入所有 application/domain/repository 细节。
- 后续若执行 API 重写，需要先与你确认是“兼容迁移”还是“直接切换到客户端协议”，否则实施边界不同。

知识沉淀 / 是否回写 docs：

- 暂不回写 `docs/`。当前仅形成任务级基线，不新增长期规则。

产物清理与保留说明：

- 保留本任务单作为后续 API 重写的追踪基线。

补充说明：

- 当前已具备进入“基于 `docs/t` 输出 API 重写方案/任务拆解”的上下文基础。
