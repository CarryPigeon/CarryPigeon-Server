/**
 * 应用启动模块（Spring Boot 入口层）。
 * <p>
 * 阅读建议：
 * <ol>
 *   <li>从 {@link team.carrypigeon.backend.starter.ApplicationStarter} 了解框架启动与扫描范围</li>
 *   <li>进入 `chat-domain.controller.web.api` 查看 HTTP/WS 入口</li>
 *   <li>再跟踪 `ApiFlowRunner -> LiteFlow chain -> 节点组件` 的执行链路</li>
 * </ol>
 */
package team.carrypigeon.backend.starter;

